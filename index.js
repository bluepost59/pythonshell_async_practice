const { PythonShell } = require("python-shell");

// PythonShell.runString(
//     'import json; print(json.dumps({"hoge":3.14}))',
//     { mode: "json" },
//     (err, result) => { console.log(result[0].hoge); });